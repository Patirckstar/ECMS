# 电商商品管理模块 UI界面设计说明文档

## 一、文档说明

本文档基于《电商商品管理模块需求规约》编写，对商品管理全部页面的**UI布局、页面结构、字段展示、交互逻辑、视觉规范、状态样式**进行标准化定义，可直接交付UI设计师出高保真UI稿、前端开发还原。

本次包含4个核心页面：
- 商品列表页
- 商品新增/编辑页
- SKU规格配置页
- 库存日志/状态管理页

---

## 二、全局UI规范

### 2.1 页面布局

整体采用**后台经典左右布局**：左侧菜单栏 + 顶部导航栏 + 主体白色内容区。

### 2.2 色彩规范

| 用途 | 颜色值 | 色系 |
|------|--------|------|
| 主色（新增、确定、保存、主要按钮） | `#409EFF` | 蓝色 |
| 警示色（删除、缺货、异常、驳回） | `#F56C6C` | 红色 |
| 成功色（上架、审核通过、正常） | `#67C23A` | 绿色 |
| 信息灰（下架、草稿、禁用） | `#909399` | 灰色 |
| 警告色（待审核、库存预警） | `#E6A23C` | 黄色 |

### 2.3 通用交互规范

- 所有输入框**失焦校验**，必填项红色 `*` 标识
- 操作按钮hover高亮、禁用状态置灰不可点击
- 弹窗居中显示、蒙层遮罩（透明度0.5）、可关闭
- 列表统一分页、排序、筛选样式
- 所有确认操作需二次弹窗确认

---

## 三、页面1：商品列表页

### 3.1 页面结构

页面按区域分为：**顶部筛选栏 → 功能按钮栏 → 数据表格区 → 底部分页栏**

### 3.2 顶部筛选栏

单行布局，从左到右依次排列：

| 筛选项 | 组件类型 | 说明 |
|--------|----------|------|
| 关键词搜索 | 输入框 | 模糊匹配商品名称/SPU编码/SKU编码 |
| 商品状态 | 下拉选择 | 全部 / 草稿 / 待审核 / 已上架 / 已下架 / 审核驳回 |
| 商品分类 | 级联下拉 | 三级分类选择 |
| 商品标签 | 多选下拉 | 新品 / 热销 / 包邮 / 自定义标签 |
| 价格区间 | 输入框组 | 最小-最大，单位元 |
| 库存区间 | 输入框组 | 最小-最大 |
| 创建时间 | 日期范围选择器 | 起止日期 |
| 操作按钮 | 按钮 | **查询**（蓝色主按钮）、**重置**（默认按钮） |

### 3.3 功能按钮栏

位于筛选栏下方，表格上方：

| 按钮 | 颜色 | 权限 |
|------|------|------|
| + 新增商品 | 蓝色主按钮 | 运营/商家/管理员 |
| 批量上架 | 默认按钮 | 运营/管理员 |
| 批量下架 | 默认按钮 | 运营/管理员 |
| 批量导出 | 默认按钮 | 运营/管理员 |
| 批量删除 | 红色警示按钮 | 仅管理员可见 |

### 3.4 列表表格字段

每一行展示一条SPU商品数据：

| 字段 | 宽度 | 说明 |
|------|------|------|
| 勾选框 | 50px | 用于批量操作 |
| 商品缩略图 | 80px | 60×60方形缩略图 |
| 商品名称 | 200px | 超长自动省略，hover悬浮展示全部 |
| SPU编码 | 150px | 只读展示 |
| 商品分类 | 120px | 显示末级分类名 |
| 售价 | 100px | 显示SKU最低售价 |
| 总库存 | 100px | 所有SKU库存之和 |
| 销量 | 100px | 总销量 |
| 商品状态 | 100px | 彩色标签（见下方状态样式） |
| 创建时间 | 160px | 格式：YYYY-MM-DD HH:mm |
| 操作 | 200px | 编辑 / 查看 / 上下架 / 审核 / 库存管理 / 删除 |

### 3.5 状态标签样式

| 状态 | 标签颜色 | 文字 |
|------|----------|------|
| 草稿 | 灰色 (`#909399`) | 草稿 |
| 待审核 | 黄色 (`#E6A23C`) | 待审核 |
| 已上架 | 绿色 (`#67C23A`) | 已上架 |
| 已下架 | 浅灰 (`#C0C4CC`) | 已下架 |
| 审核驳回 | 红色 (`#F56C6C`) | 审核驳回 |

### 3.6 行内操作按钮

| 操作 | 显示条件 | 说明 |
|------|----------|------|
| 编辑 | 草稿/已下架 | 进入编辑页 |
| 查看 | 已上架/待审核/审核驳回 | 进入详情预览 |
| 上架 | 已下架 | 弹窗确认后上架 |
| 下架 | 已上架 | 弹窗确认后下架 |
| 审核查看 | 待审核/审核驳回 | 进入审核详情 |
| 库存管理 | 全部状态 | 进入库存详情页 |
| 删除 | 已下架（仅管理员） | 强二次确认后删除 |

### 3.7 交互行为

- 点击商品名称/缩略图 → 进入商品详情预览页
- 状态切换 → 弹窗二次确认
- 删除操作 → 强二次确认弹窗（红色警告）
- 表头列支持点击排序：按价格、库存、时间、销量升降序
- 分页组件：每页默认20条，可选10/20/50/100

---

## 四、页面2：商品新增/编辑页

### 4.1 页面布局

- 顶部：**面包屑导航**（商品管理 > 新增商品 / 编辑商品）
- 主体：**分步表单**，5个步骤标签页
- 底部：**固定悬浮操作栏**

### 4.2 分步步骤

| 步骤 | 名称 | 内容概要 |
|------|------|----------|
| Step 1 | 基础信息 | 名称、分类、品牌、标签、类型 |
| Step 2 | 素材信息 | 主图、详情图、视频 |
| Step 3 | 价格物流 | 价格设置、运费、发货地 |
| Step 4 | 售后配置 | 退换货、质保 |
| Step 5 | 规格SKU | 跳转SKU规格配置页 |

### 4.3 Step 1：基础信息

| 字段 | 组件 | 校验规则 |
|------|------|----------|
| 商品名称 | 输入框 | 必填，2-60字符，实时字数提示 |
| 副标题 | 输入框 | 选填，最大200字符 |
| 商品简介 | 文本域 | 选填，最大500字符 |
| 商品类型 | 单选按钮组 | 实物商品 / 虚拟商品（切换时显示/隐藏物流模块） |
| 品牌 | 下拉选择+搜索 | 选填 |
| 三级分类 | 级联选择器 | 必填，选至第三级 |
| 商品标签 | 多选标签（色块展示） | 选填 |

### 4.4 Step 2：素材信息

| 素材类型 | 组件 | 约束 |
|----------|------|------|
| 商品主图 | 拖拽上传区 | 最多5张，首张为主图（角标标记"主图"），支持拖拽排序、预览、删除 |
| 商品详情图 | 批量上传区 | 多图上传，支持排序、批量插入 |
| 商品视频 | 视频上传区 | 单视频，MP4格式≤60秒，自动提取封面，支持预览 |

图片格式：JPG/PNG/WEBP，单张≤5MB。上传后自动压缩生成缩略图。

### 4.5 Step 3：价格与物流

**价格区**：

| 字段 | 组件 | 说明 |
|------|------|------|
| 市场价 | 数字输入框（元） | 保留2位小数 |
| 销售价 | 数字输入框（元） | 必填 |
| 会员价 | 数字输入框（元） | ≤销售价 |
| 活动价 | 数字输入框（元） | 选填，≤销售价 |
| 价格生效时间 | 时间范围选择 | 仅活动价需要 |

**物流区**（仅实物商品，虚拟商品隐藏）：

| 字段 | 组件 |
|------|------|
| 运费模板 | 下拉选择 |
| 是否包邮 | 开关 |
| 发货地 | 省市区选择器 |
| 发货时效 | 数字输入+单位（小时） |

### 4.6 Step 4：售后配置

| 字段 | 组件 | 说明 |
|------|------|------|
| 七天无理由 | 开关 | 默认关闭 |
| 质保说明 | 文本域 | 选填 |
| 退换货规则 | 富文本编辑器 | 选填 |

### 4.7 底部操作栏（固定悬浮）

| 按钮 | 说明 |
|------|------|
| 保存草稿 | 灰色按钮，保存并维持草稿状态 |
| 提交审核 | 蓝色主按钮，校验全部必填项后提交 |
| 上一步 / 下一步 | 灰色默认按钮，切换分步表单 |

### 4.8 交互行为

- 任意必填项未填 → "下一步"按钮置灰不可点击
- 切换商品类型 → 即时显示/隐藏物流字段
- 每3分钟自动保存草稿（右上角提示"已自动保存"）
- 关闭/离开页面时草稿未保存 → 弹出离开确认提示

---

## 五、页面3：SKU规格配置页

### 5.1 页面结构

上下两部分：
- **上半部分**：规格选择与组合生成区
- **下半部分**：SKU明细表格区

### 5.2 规格配置区

| 元素 | 说明 |
|------|------|
| 规格维度勾选 | 多选：颜色 / 尺寸 / 型号 / 重量 / 自定义，勾选后展示对应规格值 |
| 规格值标签 | 每个规格值显示为可选中的标签块，选中高亮，未选中灰色 |
| 自定义规格值 | " + 新增规格值 " 按钮，支持输入新规格值 |
| 生成按钮 | 点击 **"自动生成SKU组合"** 蓝色按钮，根据勾选生成笛卡尔积组合 |

### 5.3 SKU明细表格

自动根据勾选的规格组合，生成所有SKU行：

| 字段 | 说明 |
|------|------|
| 规格名称 | 自动拼接，如"红色 / M码" |
| SKU编码 | 自动生成（可手动修改） |
| 市场价 | 默认继承SPU价格，可单独修改 |
| 销售价 | 默认继承SPU价格，可单独修改 |
| 会员价 | 默认继承SPU价格，可单独修改 |
| 库存 | 数字输入框，初始为0 |
| 状态 | 开关：启用 / 禁用 |
| 操作 | 单独编辑、单独上下架 |

### 5.4 批量操作栏

位于SKU表格上方：

| 按钮 | 功能 |
|------|------|
| 批量设置价格 | 弹窗输入统一价格，覆盖全部SKU |
| 批量设置库存 | 弹窗输入统一库存，覆盖全部SKU |
| 批量启用 | 全部SKU状态设为启用 |
| 批量禁用 | 全部SKU状态设为禁用 |

### 5.5 交互行为

- 勾选/取消规格 → SKU表格实时重新生成
- 库存为0的行 → 整行背景变浅灰色，库存数字显示红色
- 所有数值字段支持行内快速编辑
- 表格可横向滚动（规格维度多时）

---

## 六、页面4：库存管理与状态日志页

### 6.1 页面结构

三个Tab标签页：
- **库存概况**（默认）
- **库存日志**
- **状态流转**

### 6.2 顶部信息卡片

展示当前SPU商品概览：

| 信息项 | 展示 |
|------|------|
| 商品名称 | 文本 |
| SPU编码 | 文本 |
| 商品状态 | 彩色标签 |
| 总库存 | 数字（所有SKU可用库存之和） |
| 在售SKU数 | 数字 |
| 缺货SKU数 | 红色数字 |

### 6.3 Tab 1：库存概况

SKU库存明细表格：

| 字段 | 说明 |
|------|------|
| SKU规格 | 规格名称 |
| SKU编码 | 只读文本 |
| 可用库存 | 数字 |
| 锁定库存 | 灰色数字（下单未付款） |
| 预警阈值 | 数字 |
| 库存状态 | 正常（绿色）/ 预警（黄色）/ 缺货（红色） |
| 操作 | **调整库存** 按钮 → 弹窗输入增减数量 |

### 6.4 Tab 2：库存日志

记录所有库存变动，以表格形式展示：

| 字段 | 说明 |
|------|------|
| 变动时间 | YYYY-MM-DD HH:mm:ss |
| 操作人 | 用户名 |
| SKU信息 | 规格名称 + SKU编码 |
| 变动数量 | 正数绿色`+N`、负数红色`-N` |
| 变动类型 | 彩色小标签：新增入库 / 手动调整 / 下单锁定 / 订单释放 / 订单扣减 |
| 变动前库存 | 数字 |
| 变动后库存 | 数字 |

支持按时间范围筛选、按变动类型筛选。

### 6.5 Tab 3：状态流转

以**时间线**形式展示商品状态变更历史：

```
2024-01-15 10:30  创建商品 → 草稿        操作人：张三
2024-01-15 11:00  提交审核 → 待审核      操作人：张三
2024-01-15 14:20  审核通过 → 已上架      操作人：李四（审核员）
2024-01-20 09:00  手动下架 → 已下架      操作人：张三
```

每条记录包含：操作时间、状态变更方向（彩色箭头）、操作人、备注。

---

## 七、通用弹窗UI

| 弹窗类型 | 样式 | 内容 |
|----------|------|------|
| 新增/编辑弹窗 | 标准居中弹窗，宽600-800px | 表单紧凑布局，底部确定/取消按钮 |
| 确认操作弹窗 | 简洁小弹窗，宽420px | 标题 + 提示文字 + 取消/确定按钮（确定按钮颜色根据场景变化） |
| 图片预览弹窗 | 全屏大图居中 | 图片最大化展示，右上角关闭按钮，支持左右切换 |
| 批量操作结果弹窗 | 中等弹窗 | 显示成功/失败数量，失败原因列表 |

---

## 八、交付清单

需UI设计师输出以下4张高保真页面：

| 编号 | 页面名称 | 包含内容 |
|------|----------|----------|
| 1 | 商品管理列表页 | 完整版含筛选栏、批量操作栏、状态标签、分页 |
| 2 | 商品新增/编辑页 | 分步表单（SPU基础信息），含5个Step完整展示 |
| 3 | SKU规格配置页 | 规格勾选区 + SKU明细表格 |
| 4 | 库存与日志详情页 | 信息卡片 + 三个Tab切换页面 |

---

> *本文档用于指导UI设计和前端开发，确保页面实现与需求规约一致*
