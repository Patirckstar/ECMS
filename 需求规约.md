# 电商商品管理模块需求规约

## 1 引言

### 1.1 文档目的

本文档为电商系统**商品管理模块**的需求规约，明确该模块的业务范围、功能需求、非功能需求、数据规范、接口约束等内容，为后续设计、开发、测试和验收提供统一标准。本文档覆盖商品从创建、编辑、审核、上架、运营到下架删除的全生命周期管理场景。

### 1.2 适用范围

本规约适用于电商后台管理系统的商品管理模块，面向平台运营人员、商家管理员、系统管理员三类角色使用。不包含前台商品展示、订单交易、售后评价等关联模块功能。

### 1.3 术语与定义

| 术语 | 定义说明 |
|------|----------|
| SPU | 标准产品单位（Standard Product Unit），代表一款商品的整体品类，如"纯棉短袖T恤"，是商品聚合维度 |
| SKU | 库存保有单位（Stock Keeping Unit），基于SPU衍生的具体规格，如"纯棉短袖T恤-黑色-M码"，是库存与定价的最小单位 |
| 商品上架 | 商品审核通过后展示于前台商城，可被用户浏览、搜索、下单 |
| 商品下架 | 商品停止前台展示，用户不可下单，仅后台可见、可编辑 |
| 实物商品 | 需要物流配送的实体货品，支持库存管控与发货物流跟踪 |
| 虚拟商品 | 无实物物流的商品，如优惠券、会员权益、线上课程等 |

### 1.4 参考文档

1. 《电商系统整体需求规格说明书》
2. 《电商系统数据字典规范》
3. 《后台管理系统通用权限规范》
4. 《图片、文件上传存储规范》

---

## 2 总体概述

### 2.1 模块目标

商品管理模块是电商平台的核心基础模块，目标是实现商品信息标准化、规范化管理，支撑运营人员高效完成商品创建、信息维护、库存管控、状态流转、分类营销配置等操作，保障前台商品展示准确、交易正常。

### 2.2 业务角色

| 角色 | 权限范围 |
|------|----------|
| 运营管理员 | 新增、编辑、上下架、库存修改、分类配置、商品详情维护，可查看统计数据，无删除权限 |
| 商家管理员 | 自有商品全流程管理：新增、编辑、上下架、库存调整，仅可操作本店铺商品 |
| 系统管理员 | 拥有商品模块全部权限，包含商品删除、权限配置、异常数据修复、批量操作 |
| 审核管理员 | 仅负责商品内容审核（通过/驳回），无编辑、发布权限 |

### 2.3 业务流程概述

1. **商品创建**：选择商品类型 → 填写SPU基础信息 → 配置规格SKU → 上传素材 → 设置价格库存 → 提交审核
2. **商品审核**：审核人员校验信息合规性 → 通过后自动上架，驳回则返回修改并备注原因
3. **商品运营**：实时编辑信息、调整库存、修改价格、切换上下架、配置分类标签
4. **商品收尾**：滞销/过期商品手动下架，无交易记录的下架商品可由管理员删除

---

## 3 功能需求

### 3.1 商品基础管理

#### 3.1.1 商品新增

支持创建实物商品和虚拟商品，必填项校验不通过禁止提交：

1. **基础信息**：商品名称（2-60字符，禁止特殊违规字符）、副标题、简介、品牌、三级分类（必填）、商品标签（自定义/系统标签）、商品类型（实物/虚拟）
2. **素材信息**：主图（最多5张，首张必填）、详情图、视频（MP4格式，≤60秒），图片支持JPG/PNG/WEBP格式，单张≤5MB
3. **价格配置**：市场价、销售价、会员价、活动价，保留两位小数，禁止负数，活动价优先级高于常规售价
4. **物流配置**（仅实物商品）：发货地、运费模板、是否包邮、发货时效
5. **售后配置**：退换货规则、质保期限、是否支持七天无理由退换

#### 3.1.2 商品编辑

| 商品状态 | 编辑规则 |
|----------|----------|
| 待审核 | 可修改全部信息，修改后需重新提交审核 |
| 已上架 | 基础信息、素材、价格、库存修改实时生效；分类、品牌修改需重新审核 |
| 已下架 | 可自由修改全部信息，修改后可直接上架或保存草稿 |

#### 3.1.3 商品删除

1. 仅系统管理员拥有删除权限
2. 已产生订单/售后记录的商品禁止删除，仅可永久下架
3. 无交易记录的下架商品支持单个/批量删除，删除后进入回收站，7天内可恢复，超时永久清除

### 3.2 SPU / SKU规格管理

#### 3.2.1 SPU管理

1. 系统自动生成唯一SPU编码，不可手动修改
2. 一个SPU可关联多个SKU，统一维护SPU公共信息（名称、主图、详情、品牌、分类）
3. 支持SPU批量绑定标签、批量修改分类、批量设置售后规则

#### 3.2.2 SKU规格配置

1. 支持自定义规格模板（颜色、尺寸、重量、型号等），可新增、编辑、删除规格项
2. 勾选规格后自动生成SKU组合，支持单独修改每个SKU的价格、库存、编码
3. 每个SKU生成唯一编码，支持单独上下架
4. SKU库存为0时自动缺货，不影响同SPU其他SKU
5. 支持SKU批量调价、批量增减库存、批量启用/禁用

### 3.3 商品状态管理

商品包含五种核心状态，流转规则如下：

| 状态 | 说明 | 可操作 |
|------|------|--------|
| 草稿 | 创建未提交审核，仅后台可见 | 编辑、提交审核 |
| 待审核 | 已提交审核，锁定核心信息 | 等待审核 |
| 审核驳回 | 审核不通过，展示驳回原因 | 修改后重新提交、保存草稿 |
| 已上架 | 审核通过，前台正常展示可下单 | 手动下架、编辑信息 |
| 已下架 | 前台隐藏不可下单 | 手动上架、编辑信息 |

**自动规则**：SKU全部库存为0时，系统自动标记商品缺货，可配置自动下架。

### 3.4 库存管理

1. 库存精确到SKU维度，支持手动设置初始库存、手动增减、批量调整
2. 支持库存预警配置，低于阈值时后台弹窗提醒并消息通知
3. **交易锁定机制**：用户下单锁定库存 → 付款成功扣除 → 订单取消自动释放
4. 库存日志记录所有变动（操作人、时间、数量、原因），支持查询导出

### 3.5 商品分类与标签管理

#### 3.5.1 分类管理

1. 支持最多三级商品分类，系统管理员可新增、编辑、排序、启用/禁用
2. 分类禁用后，关联商品自动解除绑定

#### 3.5.2 标签管理

1. 支持系统固定标签（新品、热销、爆款、包邮）和自定义标签，可设置展示颜色
2. 支持单个/批量商品绑定/解绑标签

### 3.6 商品审核管理

1. **审核维度**：商品名称合规性、图片是否违规、价格是否异常、分类匹配度、售后规则完整性、违禁内容检查
2. **审核操作**：通过（自动上架）/ 驳回（必须填写具体原因，同步反馈提交人）
3. **审核记录**：审核人、审核时间、审核结果、驳回原因永久留存，支持查询导出
4. 支持批量通过，驳回仅支持单个操作

### 3.7 商品查询与导出

1. **多条件筛选**：商品名称、SPU/SKU编码、状态、分类、标签、价格区间、创建时间、库存状态
2. **模糊搜索**：关键词匹配商品名称、简介、编码
3. **排序**：按创建时间、价格、库存、销量升降序
4. **批量导出**：Excel格式导出商品基础信息、SKU、库存、价格数据

---

## 4 非功能需求

### 4.1 性能需求

| 指标 | 要求 |
|------|------|
| 列表页加载 | ≤ 1秒 |
| 筛选/搜索响应 | ≤ 0.5秒 |
| 批量操作（≤100条） | ≤ 3秒 |
| 并发用户数 | ≥ 50人同时在线无卡顿 |
| 图片/视频上传 | ≤ 5秒 |

### 4.2 安全需求

1. **权限隔离**：不同角色严格权限区分，越权操作拦截并记录日志
2. **数据校验**：前端+后端双重校验，防止非法字符和恶意注入
3. **操作日志**：全操作留痕（新增、编辑、删除、上下架、库存修改、审核），日志永久留存
4. **数据备份**：每日自动备份，支持数据恢复

### 4.3 兼容性需求

1. **浏览器**：支持Chrome、Edge、Firefox最新版本
2. **设备**：后台系统适配PC端不同分辨率，自适应展示

### 4.4 易用性需求

1. 界面简洁清晰，功能分区明确，高频操作一键触发
2. 必填项、异常状态、操作结果有明确文字提示
3. 支持草稿自动保存，防止数据丢失

---

## 5 数据需求

### 5.1 核心数据字段

1. **SPU字段**：SPU编码、商品名称、副标题、简介、品牌ID、分类ID、商品类型、状态、创建时间、更新时间、创建人
2. **SKU字段**：SKU编码、SPU编码、规格属性、市场价、销售价、会员价、库存、锁定库存、运费模板ID、状态
3. **库存字段**：可用库存、锁定库存、预警阈值、最后变动时间
4. **审核字段**：审核状态、审核人、审核时间、驳回原因

### 5.2 数据约束

1. SPU编码、SKU编码全局唯一，自动生成，不可重复
2. 价格、库存数值非负，库存为整数，价格保留两位小数
3. 已上架商品核心信息变更需留痕

---

## 6 接口需求

| 接口 | 用途 |
|------|------|
| 商品信息查询接口 | 供前台首页、搜索、分类模块调用，获取上架商品公开信息 |
| 库存同步接口 | 与订单模块联动，实现下单锁库存、付款扣库存、取消释放库存 |
| 商品状态同步接口 | 上下架状态变更实时同步至前台和营销模块 |
| 图片上传接口 | 素材文件上传、压缩、存储、返回访问地址 |
| 数据统计接口 | 为报表模块提供商品数量、库存总量等数据 |

---

## 7 约束与假设

### 7.1 业务约束

1. 已产生订单的商品不允许删除，仅可下架
2. 审核驳回后，未修改完成禁止再次提交上架
3. 虚拟商品不支持物流和实物库存管控

### 7.2 技术约束

1. 商品素材统一存储至对象存储服务器
2. 单次查询最大数据量不超过1000条

---

## 8 验收标准

1. 所有功能需求全部实现，状态流转、权限控制、数据校验符合本文档规则
2. 页面响应、并发性能、上传速度满足非功能需求指标
3. 接口调用正常，数据同步准确，模块间联动顺畅
4. 操作日志完整留存，数据备份恢复功能可用
5. 兼容主流浏览器，界面展示正常，操作流畅

---

> *本文档基于项目需求编写，用于指导电商商品管理模块的设计与开发*
